# Workplace Safety & Fire Drill Scenario Evaluator — supporting tools
# Runtime for the knowledge updater. Pin the async HTTP client and test runner.
httpx>=0.27,<1.0
# Optional richer HTML/JS crawling for OSHA/NFPA pages (gracefully skipped if absent).
crawl4ai>=0.4.0; extra == "crawl"
# Test runner.
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0
